#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;

    cin >> n;
    cout << 2000 << '\n';
    for (int i = 0; i < 1000; ++i)cout << 1 << '\n';
    for (int i = 0; i < 1000; ++i)cout << 1000 << '\n';

    return 0;
}